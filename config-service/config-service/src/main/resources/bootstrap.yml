server:
  port: 8081
spring:
  security:
    user:
      name: root
      password: s3cr3t
  cloud:
    config:
      server:
        git:
          uri: https://github.com/renzodamian/demoMicroservicioStore.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}



