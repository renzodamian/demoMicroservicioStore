server:
  port: 8081
spring:
  security:
    user:
      name: root
      password: s3cr3t
  cloud:
    config:
      server:
        bootstrap: true
        git:
          uri: https://github.com/renzodamian/demoMicroservicioStore
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}